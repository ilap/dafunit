# Installation Instructions

## Dependencies:
  - debhelper (>= 7.0.50~)
  - cmake (>= 2.8)
  - libglib2.0-dev (>= 2.32)
  - valac (>= 0.16)
  - libgee-dev (>= 0.6.8)

# Project dependencies
  - dafunit

# Build:
```bash
meson build && cd build
ninja
sudo ninja install

#Uninstall 
$ sudo ninja uninstall
```

